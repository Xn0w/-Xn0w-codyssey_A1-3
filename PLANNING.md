# 서비스 기획서 — CodeMemo

## 1. 목적

CS 전공 학생이 과제나 팀 프로젝트 코드를 제출하기 전, 사람에게 보여주기 전 단계에서
빠르게 1차 피드백을 받을 수 있는 코드 리뷰 도구를 제공한다. 정식 코드 리뷰어를 대체하는
것이 아니라, 제출 전 "빠뜨린 부분은 없는지" 스스로 점검하는 보조 도구다.

## 2. 타겟 사용자

- 과제/프로젝트 코드를 정기적으로 제출하는 컴퓨터공학 전공 학생
- 팀 프로젝트에서 PR을 올리기 전 스스로 먼저 점검하고 싶은 사용자
- 새로운 언어·문법을 배우는 중이라 관용적인 코드 스타일인지 확인받고 싶은 사용자

## 3. 페이지 구성

| 페이지 | 역할 |
|---|---|
| 홈 (`index.html`) | 서비스 소개, 사용 시나리오 3가지, 리뷰 결과 예시 미리보기 |
| AI 코드 리뷰 (`review.html`) | 실제 AI 기능이 동작하는 핵심 페이지 |
| 가이드 (`guide.html`) | 사용 방법 3단계, 오류/지연 상황별 안내 |

세 페이지는 상단 고정 내비게이션 바로 서로 이동 가능하다.

## 4. 핵심 기능 (AI 기능 정의)

- **입력**: 리뷰받을 코드(텍스트), 코드가 작성된 언어(선택)
- **처리**: 백엔드가 Gemini API에 "시니어 개발자 리뷰어" 역할을 지시하는 프롬프트와 함께
  코드를 전달하고, 정해진 JSON 스키마(`score`, `findings[]`)로만 응답하도록 강제한다.
- **출력**: 0~100점의 종합 점수와, 좋은 점(+) / 주의할 점(!) / 문제(-)로 구분된
  2~5개의 구체적 피드백 목록
- **사용자에게 주는 가치**: 사람에게 코드를 보여주기 전에 스스로 한 번 더 점검할 수
  있는 짧고 실행 가능한 피드백을 즉시 얻는다.

## 5. 입력/출력/실패 처리 기준

| 상황 | 처리 |
|---|---|
| 정상 입력 | 점수 카드 + 피드백 리스트 렌더링 |
| 빈 입력(코드 미입력) | API 호출 없이 즉시 "코드를 입력해주세요" 안내 |
| API 오류 (4xx/5xx) | 서버가 반환한 오류 메시지를 화면에 그대로 안내 |
| 응답 지연/타임아웃 (20초 초과) | 요청 자동 취소 후 재시도 안내 |
| AI 응답이 정해진 JSON 형식이 아님 | "해석할 수 없는 형식" 오류로 안내 (원문을 그대로 노출하지 않음) |

## 6. 보안/운영 고려사항

- `GEMINI_API_KEY`는 코드/README/스크린샷 어디에도 노출하지 않고 Vercel 환경 변수로만 관리한다.
- 과금 보호를 위해 코드 입력 길이를 6,000자로 제한한다.
- 백엔드 함수의 `maxDuration`을 25초로 설정해, 프론트엔드의 20초 타임아웃보다 여유를 둔다.

## 7. 향후 보너스 아이디어 (미구현)

- 리뷰 결과를 Google Sheets 등 외부 저장소에 자동 기록해 "내가 자주 하는 실수" 통계 축적
- 다크모드 대비 라이트모드 토글(현재는 다크 테마 고정)
